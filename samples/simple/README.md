# Simple example
This is simple example for using SuperBus with basic Rebus infrastructure.

## Run tests
Just run `Start-Services.ps1`. It will build the necessary project and start everything with docker compose.